<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/main_background">

    <!-- Title -->
    <TextView
        android:id="@+id/text_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Inventario de productos"
        android:textSize="24sp"
        android:fontFamily="@font/urbanist_extra_bold"
        android:textColor="@color/white"
        android:padding="15dp"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp" />

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_products"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/text_title"
        android:layout_marginTop="20dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="8dp"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        android:scrollbars="vertical" />

    <!-- Floating Action Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_product"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"

        android:background="@drawable/fab_shape"
        android:backgroundTint="@color/spinner_black"
        android:scrollbarThumbHorizontal="@color/background_transparent"
        android:scrollbarThumbVertical="@color/background_transparent"
        android:scrollbarTrackHorizontal="@color/background_transparent"
        android:scrollbarTrackVertical="@color/background_transparent"
        android:src="@android:drawable/ic_menu_add"
        app:fabSize="normal" />

</RelativeLayout>